#!/bin/bash

[ -z $1 ] && {
    echo "add target"
    exit 1
}

img=$@
set --
NUNC_HOME="/home/amoiseev/storage/work/nuance-master"

cd ${NUNC_HOME}
pwd
source poky/oe-init-build-env

bitbake $img -c cleanall
bitbake $img
